"use client";import{useState}from"react";import{useRouter}from"next/navigation";import Link from"next/link";export function AuthForm({mode}:{mode:"login"|"register"}){const router=useRouter(),[error,setError]=useState(""),[pending,setPending]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setError("");setPending(true);const data=new FormData(event.currentTarget);const response=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(data))});const result=await response.json();setPending(false);if(!response.ok){setError(result.error??"Une erreur est survenue");return}router.push("/dashboard");router.refresh()}return <main className="auth"><section className="auth-card"><p className="eyebrow">MuscuPro Global</p><h1>{mode==="login"?"Connexion":"Créer un compte"}</h1><form onSubmit={submit}>{mode==="register"?<label className="field">Nom<input name="name" autoComplete="name" required/></label>:null}<label className="field">Courriel<input name="email" type="email" autoComplete="email" defaultValue={mode==="login"?"athlete@muscupro.local":""} required/></label><label className="field">Mot de passe<input name="password" type="password" minLength={8} autoComplete={mode==="login"?"current-password":"new-password"} defaultValue={mode==="login"?"Demo1234!":""} required/></label>{error?<p role="alert" style={{color:"#ff9d9d"}}>{error}</p>:null}<button className="button" type="submit" disabled={pending}>{pending?"Patientez…":mode==="login"?"Se connecter":"Commencer"}</button></form><p className="muted">{mode==="login"?<>Nouveau? <Link href="/register">Créer un compte</Link></>:<>Déjà membre? <Link href="/login">Se connecter</Link></>}</p></section></main>}
