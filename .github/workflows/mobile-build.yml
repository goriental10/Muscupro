name: Mobile build
on: { workflow_dispatch: {} }
permissions:
  contents: write
  pull-requests: write
jobs:
  mobile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Configure EAS credentials before production build"
