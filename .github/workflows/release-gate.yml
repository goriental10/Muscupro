name: Release gate
on: { workflow_dispatch: {} }
permissions:
  contents: write
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/final-validate.mjs
