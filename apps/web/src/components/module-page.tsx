import { Sidebar } from "./sidebar";
export type ModuleItem={title:string;detail:string;tag?:string};
export function ModulePage({eyebrow,title,description,items}:{eyebrow:string;title:string;description:string;items:ModuleItem[]}){return <div className="shell"><Sidebar/><main className="main"><header className="top"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="muted">{description}</p></div><button className="button" type="button">Ajouter</button></header><section className="module-grid">{items.map((item,index)=><article className="card module-card" key={`${item.title}-${index}`}><div className="icon" aria-hidden="true">{['◆','●','▲'][index%3]}</div><h2>{item.title}</h2><p className="muted">{item.detail}</p>{item.tag?<span className="good">{item.tag}</span>:null}</article>)}</section></main></div>}
