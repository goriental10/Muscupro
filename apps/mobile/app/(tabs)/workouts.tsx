import { useEffect,useState } from "react"; import { StyleSheet,Text } from "react-native"; import { Screen } from "../../src/components/Screen"; import { Card } from "../../src/components/Card"; import { api } from "../../src/lib/api";
export default function Workouts(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any>("/api/v1/workouts?limit=20").then(d=>setRows(d.workouts??d.items??[])).catch(()=>{});},[]);return <Screen>{rows.length?rows.map((w:any)=><Card key={w.id} title={w.title??"Séance"}><Text style={s.t}>{w.status??"PLANNED"}</Text><Text style={s.m}>{w.scheduledAt?new Date(w.scheduledAt).toLocaleDateString("fr-CA"):""}</Text></Card>):<Text style={s.m}>Aucune séance chargée.</Text>}</Screen>} const s=StyleSheet.create({t:{color:"white"},m:{color:"#94a3b8"}});
